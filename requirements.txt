# Dépendances de l'application de gestion de projet
# Streamlit >= 1.43 requis pour le paramètre width='stretch'
streamlit>=1.43.0
plotly>=5.18.0
pandas>=2.0.0
# Stockage des données : document JSON persisté dans un gist GitHub (en ligne)
# ou un fichier local (en développement). 'requests' sert à l'API GitHub Gist.
requests>=2.28.0
# Export d'images/PDF des graphiques Plotly (optionnel mais recommandé).
# La version 0.2.1 embarque Chromium et fonctionne sans navigateur externe ;
# les versions >=1.0 nécessitent une installation séparée de Google Chrome.
kaleido==0.2.1
